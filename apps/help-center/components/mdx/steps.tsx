// Numbered step-by-step instructions. We re-export Fumadocs' own Steps/Step so
// MDX authors get a single, documented import surface alongside our custom
// components — no need to reimplement the styling.
export { Step, Steps } from 'fumadocs-ui/components/steps';
